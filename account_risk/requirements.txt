# Streamlit 网页框架
# 钉在 1.49.1：1.5x 起 st.components.v1.html(iframe) 有渲染回归（切换记录时空白）+ use_container_width 弃用；
# 1.49.1 稳定可用。升级前务必先验证时间线表格能正常渲染。
streamlit==1.49.1

# 数据处理 + Excel/CSV 导出
pandas
openpyxl

# 读取 .env 文件
python-dotenv

# Kyuubi / Hive 连接：pyhive 通过 thrift + thrift_sasl 走 LDAP
# 不要装 sasl（C 扩展，Py3.10+ 编译挂）；thrift_sasl 会自动用 pure-sasl 纯 Python 实现
pyhive
thrift
thrift_sasl
